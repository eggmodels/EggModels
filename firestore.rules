rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Allow anyone to read tennis_odds/current
    match /tennis_odds/{document=**} {
      allow read;
    }

    // Deny all other access by default
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
